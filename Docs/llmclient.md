                 LLMClient
                    │
          ┌─────────┼─────────┐
          ↓         ↓         ↓
       OpenAI      Groq     Local

we are flow this llm client architeture so when we required to change between model it can easy for use.
llmclient will take prompt as input and give string as output.